services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: codereviewer
      POSTGRES_PASSWORD: codereviewer
      POSTGRES_DB: codereviewer
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
